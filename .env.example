# SQLite path. Use an absolute persistent-volume path when deploying.
DATABASE_URL=sqlite:///./data/patients.db
# Comma-separated origins for a browser dashboard, if one is added later.
ALLOWED_ORIGINS=
