# PostgreSQL URL from your hosting provider's internal database connection details.
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DATABASE
# Comma-separated origins for a browser dashboard, if one is added later.
ALLOWED_ORIGINS=
